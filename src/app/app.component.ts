import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: 'app.component.html',
  //con el template podemos definir directamente lo que queremos que aparezca con
  //código html, no redireccionando a una url
  //template:
  styleUrls: ['app.component.css']
})
export class AppComponent {
 


}
